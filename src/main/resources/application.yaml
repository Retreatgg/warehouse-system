spring:
  datasource:
    url: jdbc:postgresql://localhost:5050/shoro_warehouse_db?stringtype=unspecified
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:/db/changelog/master.yaml


  freemarker:
    check-template-location: false

server:
  port: 9999


